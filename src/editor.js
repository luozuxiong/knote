export default class KNote {
    domElement = null
    options = {}

    constructor(domElement, options) {
        this.domElement = domElement;
        this.options = options;
    }

    show() {

    }

}